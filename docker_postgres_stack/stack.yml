# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: novae_poc_2021

  adminer:
    image: adminer
    restart: always
    ports:
      - 6000:8080